export interface Service {
  id: string
  title: string
  description: string
  url: string
  logo: string
  category?: string
  section?: string
}

export interface HeaderConfig {
  title: string
  subtitle: string
}
